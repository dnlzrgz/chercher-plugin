[project]
name = "chercher-plugin-ingest"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "cookiecutter>=2.6.0",
]

[dependency-groups]
dev = [
    "commitizen>=4.6.0",
    "pytest>=8.3.5",
    "ruff>=0.11.5",
]
